mainmodule ross {

	extern module pe; /**< defines the PE */
	extern module lp; /**< defines the LP */

  readonly CProxy_PE pes;
  readonly CProxy_LP lps;

	/** \brief Main chare needed for Charm compilation */
	mainchare Main {
    entry Main(CkArgMsg *);
		entry Main(CkMigrateMsg *);
	};

	/** \brief Common readonlies should be added */
}
