mainmodule charm_setup {
  readonly CProxy_Initialize mainProxy;
  mainchare Initialize {
    entry [notrace] Initialize(CkArgMsg *);
    entry [notrace,reductiontarget] void Exit();
  };

  initnode registerStatsReduction();

  extern module event;
  extern module gvtmanager;
  extern module lp;
  extern module scheduler;
  extern module PrefixLB;
}
