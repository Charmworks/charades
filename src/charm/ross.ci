mainmodule ross {
  readonly CProxy_Initialize mainProxy;
  /** \brief Main chare that initializes pes */
  mainchare Initialize {
    entry [notrace] Initialize(CkArgMsg *);
    entry [notrace,reductiontarget] void Exit();
  };
  extern module pe;
  extern module gvtsynch;
  extern module lp;
  extern module event;
}
