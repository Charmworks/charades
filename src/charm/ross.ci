mainmodule ross {

  extern module pe; /**< defines the PE */
  extern module lp; /**< defines the LP */

  readonly CProxy_PE pes;
  readonly CProxy_LP lps;

  /** \brief Main chare that initializes pes */
  mainchare Initialize {
    entry Initialize(CkArgMsg *);
    entry Initialize(CkMigrateMsg *);
    entry [reductiontarget] Exit();
  };
}
