module lp {
  extern module pe;
  extern module event;

  array [1D] LP {
    entry LP(); /**< constructor */
    entry void init();
    entry [reductiontarget] void stopScheduler(); /**< Reduction target to stop the scheduler after LPs have been created */

    entry [expedited] void recv_remote_event(RemoteEvent*); /**< recieve an event for this LP */
    entry [expedited] void recv_anti_event(RemoteEvent*);
  };
};
