module lp {
  extern module event;

  readonly CProxy_LP lps;

  array [1D] LP {
    entry LP(); /**< constructor */
    entry void init();
    entry [reductiontarget] void stopScheduler(); /**< Reduction target to stop the scheduler after LPs have been created */

    entry void recv_event(RemoteEvent*); /**< recieve an event for this LP */
  };
};
